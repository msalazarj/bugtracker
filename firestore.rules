rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    //============================================================
    // REGLAS DE PERFILES DE USUARIO (CORREGIDO)
    //============================================================
    match /profiles/{userId} {
      // CUALQUIER usuario logueado puede LEER los perfiles de otros.
      allow read: if request.auth != null;
      
      // SOLO el propio usuario puede CREAR, ACTUALIZAR o BORRAR su perfil.
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    //============================================================
    // REGLAS DE PROYECTOS
    //============================================================
    match /projects/{projectId} {
      allow read, update: if request.auth != null && request.auth.uid in resource.data.members;
      allow create: if request.auth != null && request.auth.uid in request.resource.data.members;
      allow delete: if request.auth != null && request.auth.uid == resource.data.ownerId;
    }

    //============================================================
    // REGLAS DE BUGS
    //============================================================
    match /bugs/{bugId} {
      function isProjectMember(projectId) {
        return get(/databases/$(database)/documents/projects/$(projectId)).data.members.hasAny([request.auth.uid]);
      }
      
      allow read, update, delete: if request.auth != null && isProjectMember(resource.data.proyecto_id);
      allow create: if request.auth != null && isProjectMember(request.resource.data.proyecto_id);
    }
    
    //============================================================
    // REGLAS DE EQUIPOS (TEAMS)
    //============================================================
    match /teams/{teamId} {
      allow read, update: if request.auth != null && request.auth.uid in resource.data.members;
      allow create: if request.auth != null && request.auth.uid in request.resource.data.members;
      allow delete: if request.auth != null && request.auth.uid == resource.data.ownerId;
    }
  }
}
