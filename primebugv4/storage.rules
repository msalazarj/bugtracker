rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite subir y descargar archivos a cualquier usuario logueado
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}